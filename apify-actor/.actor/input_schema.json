{
  "title": "Lead Generator Worker Input",
  "type": "object",
  "schemaVersion": 1,
  "required": ["runId"],
  "properties": {
    "runId": {
      "title": "Run ID",
      "type": "string",
      "description": "UUID of the run to process from the Supabase runs table."
    }
  }
}
